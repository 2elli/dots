#!/usr/bin/env bash
set -e
[ -n "$PYENV_DEBUG" ] && set -x

program="${0##*/}"

export PYENV_ROOT="/home/morgan/.pyenv"
export PYENV_VERSION=$(sed 's/py//g' <<< $program)
exec "pyenv" exec python "$@"
